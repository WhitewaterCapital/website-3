"""Vendor adapters. One class per vendor — see `base.py` for the interface
every adapter implements and `../../README.md` for the extension point to
plug in a real vendor."""

from __future__ import annotations
