"""Validation layer: purged + embargoed walk-forward CV, metrics, and the
walk-forward harness that ties them together and enforces the "only claim
victory when you actually beat the baseline" rule (see harness.py)."""
