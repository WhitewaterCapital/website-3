# WW-CHAOS engine — dependencies.
#
# This sandbox has no PyPI access; all of these are already available in the
# environment the tests were built and run against. Listed here so a real
# deployment target knows what to install.

pandas>=2.0
numpy>=1.24
scipy>=1.10          # bipower/tripower variation (jump indicator), gamma function
scikit-learn>=1.3    # CHAOS-02's gradient-boosted classifier + isotonic calibration

# Test runner (this sandbox uses an offline pytest-compatible shim instead —
# see ../../CLAUDE.md / the repo's test instructions).
pytest>=7.4

# --- NOT used, on purpose (see README "Known simplifications") ---
# torch / tensorflow — the design doc's real CHAOS-02 model is a causal
# dilated TCN. No deep-learning framework is installed here and there is no
# network access to install one, so CHAOS-02 substitutes a calibrated
# gradient-boosted tree classifier instead. See chaos/directional.py.
